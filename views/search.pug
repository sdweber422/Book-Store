extends index
block content
  +previousNext
  +booklist
  +previousNext


mixin previousNext
  .previousNext
    if page > 1
      a(href='/search?options='+options +'&page='+(page-1)) Previous
    if !lastPageFlag
      a(href='/search?options='+options +'&page='+(page+1)) Next
